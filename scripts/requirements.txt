# Python parity toolchain (roadmap §2.1).
#
# These are the oracle: the scripts/pin_*.py generators load the real models here
# and dump golden fixtures into testdata/, which the Go tests then pin against. The
# discipline the repo lives by — every model-touching Go path is checked
# bit-for-bit-ish against this reference — needs an environment that has it.
#
# CPU-only; runnable on a Mac. Recreate the venv (not committed — see .gitignore):
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r scripts/requirements.txt
#
# Versions pinned to what produced the committed goldens (June 2026).
torch==2.12.0
sentence-transformers==5.5.1
transformers==5.11.0
safetensors==0.8.0
tokenizers==0.22.2
numpy==2.4.6
model2vec==0.8.2

# Only needed to regenerate the GGUF dequant goldens (pin scripts that import
# `gguf`); install on demand: pip install gguf
